<%= form_with model: @task, url: task_path(@task.id) do |f| %>
  <%= f.label :action %>
  <%= f.text_field :action %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit action_type %>
<% end %>
